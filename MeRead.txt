下一步：
优化渲染架构，实现mesh类
考虑放弃mesh合并以实现动态加载资源和更灵活的渲染

完善UI系统，然后为UI系统添加文字渲染功能；
实现完整的UI系统后，尝试开发地图编辑功能；
有地图编辑功能后，尝试ECS架构和群体寻路、避障算法；

已知问题：
纹理装箱装不下时似乎不会创建新的纹理图集；
每个模型只支持单个动画和单个色彩纹理；

在blender中，动画插值方式为非线性时，导出的模型动画会在sampler.output中记录每一帧的变化数据，
这将导致关键帧（即input）的数量与output数量不是一一对应的，
是否要考虑支持非线性动画是一个值得思考的问题。