下一步：
先实现UI系统（文字渲染），提供存档管理界面，再实现存档系统。

之后才添加树这种结构体?

已知问题：
纹理装箱装不下时似乎不会创建新的纹理图集；


现在动画确实被正确的渲染了出来，但哪怕是以最基础的动画系统为目标，依然存在着非常多需要做的工作吧，比如实体移动时应该面向它的移动方向，而不是迈克尔杰克逊太空步，以及还需要更加完善的动画状态管理系统，多动画的融合、淡入、淡出之类的，但现在这些工作其实并不好开展，因为在那之前我还要先学习Blender做一些方便调试的动画模型，最起码需要包含闲置、移动、攻击三种动画。在继续推进动画系统前，我想做点别的来放松下心情。


在blender中，动画插值方式为非线性时，导出的模型动画会在sampler.output中记录每一帧的变化数据，
这将导致关键帧（即input）的数量与output数量不是一一对应的，
是否要考虑支持非线性动画是一个值得思考的问题。

这里留个遗言，如果我死了，
https://github.com/NeonMedusa/ZigGame
这个仓库的所有代码都归仓库的其他的参与者、合作者所有，他们拥有这些代码的一切处置权。


Zig 没有 range 版的 float 随机函数，用 min + r.float(f32) * (max - min) 实现。
例：0.03 + r.float(f32) * 0.07 → [min是0.03, max是0.10)。

std.Random.DefaultPrng 的 seed 直接决定了产生序列的确定性。同样的 seed，
在不同机器、不同 Zig 版本上产生的结果一致（前提是 DefaultPrng 的实现不变）。
